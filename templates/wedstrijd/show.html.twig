{% extends 'base.html.twig' %}

{% block title %}Wedstrijd{% endblock %}

{% block body %}
    <h1>Wedstrijd</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ wedstrijd.id }}</td>
            </tr>
            <tr>
                <th>Datumtijd</th>
                <td>{{ wedstrijd.datumtijd | date('d-m-Y H:i') }}</td>
            </tr>
            <tr>
                <th>Omschrijving</th>
                <td>{{ wedstrijd.omschrijving }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('wedstrijd_index') }}">back to list</a>

    <a href="{{ path('wedstrijd_edit', {'id': wedstrijd.id}) }}">edit</a>

    {{ include('wedstrijd/_delete_form.html.twig') }}
{% endblock %}
